//
// Created by Dhru on 25/05/2018.
//

/*
#include <mem.h>

struct symbol_table{
    int address[];
    char *label[];
    int size;
};

void insert(struct symbol_table t, int address, char *label){
  t.address[t.size] = address;
  t.label[t.size] = label;
  t.size++;
}

int get(struct symbol_table t, char *label){
  for(int i = 0; i < t.size; i++){
    if(strcmp(t.label[i], label)){
      return t.address[i];
    }
  }
  return -1;
}*/






